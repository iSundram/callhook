# callhook — web

The callhook frontend application: the real war-room UI for events, calls
and campaigns — replacing the interim dashboard embedded in the Go server.

Talks to the backend's HTTP API (`../backend`): sessions, campaigns,
metrics. See `backend/README.md` for the integration surface.

**Status: scaffold — development starting.**
